let value;
const num1=100;
const num2=50;


value=num1+num2;

value= num1-num2;
value=num1*num2;
value= num1/num2;
value= num1 % num2;
//for finding the reminder

//math object
value=Math.PI;
value=Math.E;

value=Math.round(2.4);
value=Math.round(2.6);

//to round up : up and down
value=Math.ceil(2.4);
value=Math.floor(2.4);

value=Math.sqrt(64);
value=Math.abs(-3);

value=Math.pow(8,2);

value =Math.min(1,222,44,-2,64);
value =Math.max(1,222,44,-2,64);

value=Math.random();
// selects the number between 0 and 1
value=Math.random() *20 +1;
//random value between 0 and 20 

value= Math.floor(Math.random()*20+1);
// random value between 0 and 20 rounded dowm

//output
console.log(value);